# На этом шаге зависимостей нет — весь код использует только
# стандартную библиотеку Python (http.server, urllib, json, sqlite3).
#
# Когда на следующих шагах понадобятся внешние вызовы (Keenable,
# OpenRouter/clavis.to), они тоже будут через urllib — без requests.
#
# Если сюда всё же добавится пакет — фиксируем точную версию (==),
# без диапазонов (>=, ~=), чтобы пересборка образа не подтягивала
# неожиданно новый мажорный/минорный релиз.
