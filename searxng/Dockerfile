# Отдельный Railway-сервис (не тот же контейнер, что бот). Deploy this
# Dockerfile Path как "searxng/Dockerfile" при создании нового сервиса
# в том же Railway-проекте.
FROM searxng/searxng:latest

COPY searxng/settings.yml /etc/searxng/settings.yml
